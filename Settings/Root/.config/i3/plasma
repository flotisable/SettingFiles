# i3 config file (v4)
# for KDE Plasma
bar
{
  mode invisible
}
# window rules
# remove kscreen component, background image need to be handled by other
# program, e.g. feh
for_window [title="桌面 — Plasma"] kill
# vim: filetype=i3config
